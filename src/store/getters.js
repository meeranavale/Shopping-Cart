export const products = state => {
  return state.products;
};

export const cartSize = state => {
  return state.cartItems.length;
};

export const cartItems = state => {
  return state.cartItems;
};
